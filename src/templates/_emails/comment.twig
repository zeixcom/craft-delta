{% autoescape false %}
{{ deltaKeys.emailHiName|t('craft-delta', { name: author.friendlyName }) }}

{{ deltaKeys.emailCommentIntro|t('craft-delta', { commenter: commenter }) }}

  {{ entry.title }}
  {{ url }}

{{ comment }}

{{ deltaKeys.emailSignature|t('craft-delta') }}
{% endautoescape %}
